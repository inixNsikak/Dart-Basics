/**void main(List<String> args) {
  
const item  = MenuItem.settings;

  switch (item) {
    case MenuItem.settings:
      print('Open setting');
      break;
    case MenuItem.profile:
      print('Open profile');
      break;
    default:
      print('Invalid');
  }
  Enum MenuItem {
    profie,
    setting,
  }
}
**/